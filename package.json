{
  "name": "little-things",
  "private": true,
  "scripts": {
    "build": "node scripts/check-parity.mjs && turbo build",
    "check:parity": "node scripts/check-parity.mjs",
    "dev": "turbo dev",
    "css:build": "pnpm --filter kami-css build",
    "css:watch": "pnpm --filter kami-css dev"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  }
}
